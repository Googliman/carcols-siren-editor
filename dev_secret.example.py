"""Copy this file to dev_secret.py (which is gitignored - never committed) and set
your own values. The real dev_secret.py is only present on the developer's own
machine, so it never appears in the public repo history, even though this project
is open source."""

DEV_KEY_SALT = "replace-this-with-your-own-random-secret-string"
DEV_EMAIL = "your-email@example.com"
